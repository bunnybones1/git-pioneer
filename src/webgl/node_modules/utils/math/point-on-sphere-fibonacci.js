var phi = (Math.sqrt(5) + 1) * 0.5 - 1;
var ga = phi * Math.PI * 2;
function pointOnSphereFibonacci(index, total) {
	//[long, lat];
	return [ga * index, Math.asin(-1 + 2 * index / total)];
}

module.exports = pointOnSphereFibonacci;