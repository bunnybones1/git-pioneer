var three = require("three");

var __geoms = {};
function gunBase(radiusA = 0.125, radiusB = 0.125, length = 1, segsCircle = 16, segsLength = 1) {
	var hash = Array.prototype.join.call(arguments, ";");
	if(!__geoms[hash]) {
		__geoms[hash] = new three.CylinderGeometry(radiusA, radiusB, length, segsCircle, segsLength);
	}

	return __geoms[hash];
}

module.exports = gunBase;