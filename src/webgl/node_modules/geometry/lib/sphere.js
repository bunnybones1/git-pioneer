var three = require("three");

var __geoms = {};
function sphere(radius = 0.5, segsU = 16, segsV = 8) {
	var hash = Array.prototype.join.call(arguments, ";");
	if(!__geoms[hash]) {
		__geoms[hash] = new three.SphereGeometry(radius, segsV, segsV);
	}

	return __geoms[hash];
}

module.exports = sphere;