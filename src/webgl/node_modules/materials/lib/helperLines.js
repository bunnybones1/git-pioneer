var three = require("three");

var __mats = {};
function helperLines(color = 0xffffff) {
	var hash = Array.prototype.join.call(arguments, ";");
	if(!__mats[hash]) {
		__mats[hash] = new three.LineBasicMaterial({
			color: color
		});
	}

	return __mats[hash];
}

module.exports = helperLines;